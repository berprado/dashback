# Example connections file (SAFE TO COMMIT)
#
# Copia este archivo a `.streamlit/secrets.toml` y reemplaza los placeholders.
# Nota: NO subas `.streamlit/secrets.toml` al repo (está en .gitignore).

# Development connection (example)
[connections.mysql]
type = "sql"
url = "mysql+mysqlconnector://USER:PASSWORD@HOST:3306/adminerp_copy"

# Production connection (example)
# [connections.mysql_prod]
# type = "sql"
# url = "mysql+mysqlconnector://USER:PASSWORD@HOST:PORT/adminerp"
